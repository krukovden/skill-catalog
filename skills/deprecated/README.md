# deprecated

Skills no longer maintained, kept for reference and for the history of what was tried.

**Not promoted**: nothing here reaches a user — see [in-progress/README.md](../in-progress/README.md)
for how the bucket enforces that. Retiring a skill is a `git mv` into this folder plus
`npm run build`; deleting it outright loses the record of why it existed.

_(empty)_
